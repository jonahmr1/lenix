since qb-menu doesn't support images in their menus, this will be as a todo if you want to use another resources for that in the future

on multiple player previewing a car bug

cleaner error handling