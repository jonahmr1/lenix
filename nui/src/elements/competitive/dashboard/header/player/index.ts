import { useDiv } from "@trippler/tr_kit/nui"

useDiv({
  parent: "header",
  id: "player-details",
  style: "w-[40%] h-[75%] flex items-center justify-around"
})

import('./card')
import('./coins')
import('./money')
import('./friends')