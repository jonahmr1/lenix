import { useDiv } from "@trippler/tr_kit/web"

useDiv({
  parent: "header",
  id: "player-details",
  className: "w-[40%] h-[75%] flex items-center justify-around"
})

import('./card')
import('./coins')
import('./money')
import('./friends')